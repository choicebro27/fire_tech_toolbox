<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <!-- ── App metadata ─────────────────────────────────────────────── -->
    <key>CFBundleName</key>
    <string>Fire Tech Toolbox</string>

    <key>CFBundleDisplayName</key>
    <string>Fire Tech Toolbox</string>

    <!-- ── Microphone permission (dB Meter) ─────────────────────────── -->
    <key>NSMicrophoneUsageDescription</key>
    <string>Fire Tech Toolbox uses the microphone to measure real-time sound pressure levels for testing fire alarm sounder compliance with AS 1670.1. No audio is recorded or stored.</string>

    <!-- ── Document folder access (PDF standard uploads) ────────────── -->
    <key>NSDocumentsFolderUsageDescription</key>
    <string>Fire Tech Toolbox needs access to your documents folder to import Australian Standard PDF files for the AI Standards assistant.</string>

    <!-- ── Files app / iCloud Drive browsing (file_picker) ──────────── -->
    <key>UISupportsDocumentBrowser</key>
    <true/>

    <!-- ── flutter_gemma: required for model.bin to be accessible ─────── -->
    <key>UIFileSharingEnabled</key>
    <true/>

    <!-- ── Allow PDFs to be opened in-place from the Files app ──────── -->
    <key>LSSupportsOpeningDocumentsInPlace</key>
    <true/>

    <!-- ── Standard Flutter bundle keys ─────────────────────────────── -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleShortVersionString</key>
    <string>$(FLUTTER_BUILD_NAME)</string>

    <key>CFBundleSignature</key>
    <string>????</string>

    <key>CFBundleVersion</key>
    <string>$(FLUTTER_BUILD_NUMBER)</string>

    <key>LSRequiresIPhoneOS</key>
    <true/>

    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <key>UIMainStoryboardFile</key>
    <string>Main</string>

    <!-- ── Supported orientations (iPhone) ──────────────────────────── -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- ── Supported orientations (iPad) ────────────────────────────── -->
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>

    <key>CADisableMinimumFrameDurationOnPhone</key>
    <true/>

    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

</dict>
</plist>
